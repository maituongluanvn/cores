{
  "name": "cores",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:packages": "turbo run build",
    "build:ci": "turbo run build:ci",
    "testunit": "turbo run testunit",
    "dev": "turbo run dev",
    "dev:backend": "turbo run dev --filter=@cores/backend...",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "1.11.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maituongluanvn/cores.git"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "yarn": "3.2.0",
    "node": "^18.19.0",
    "npm": "Use yarn instead"
  },
  "packageManager": "yarn@3.2.0"
}
